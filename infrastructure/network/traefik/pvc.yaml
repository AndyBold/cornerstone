kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: traefik
  namespace: kube-system
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi